export interface IAchievement {
  record: {
    achievedAt: string;
    id: string;
    name: string;
  };
  type: {
    description: string;
    icon: string;
    id: string;
    name: string;
  };
}
