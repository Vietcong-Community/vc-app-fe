import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  title: {
    id: 'app.containers.League.SeasonDetail.components.MapListModal.title',
    defaultMessage: 'Seznam map v rámci sezóny',
  },
});
