import styled from 'styled-components';

export const Title = styled.div`
  font-size: 18px;
  font-weight: 500;
`;

export const Image = styled.img`
  aspect-ratio: 3/2;
  border-radius: 8px;
  width: 100%;
`;
