import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  addButton: {
    id: 'app.containers.League.MatchDetail.components.AddComment.addButton',
    defaultMessage: 'Přidat',
  },
});
